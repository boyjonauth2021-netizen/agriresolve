# Required for AI Green Assistant (chat + voice transcription)
OPENAI_API_KEY=sk-your-key-here

# Optional: override the OpenAI model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Server port (default: 5000)
PORT=5000
